import "reflect-metadata";

export class requestFilterParametersModel {
  filterCriteria!: Object;
  page!: number;
  pageSize!: number;
  filterType!: string;
  fetchType!: string;
  searchKey!: string;
  businessUnitId!: string;
}
