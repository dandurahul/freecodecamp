// f = (n) => new Array(n).fill(" ").reduce((p, c, i, arr) => p+c.repeat(arr.length - i-1) + "@".repeat(2 * i)+"\n", "")

// // console.log(f(1));
// // console.log(f(2));
// console.log(f(5));

// const data= ["rahul","rajesh"]
// data.map((e,i,arr)=>console.log(e))

let response = {
  token: "sfafda",
  responseCode: "SUCCESS",
};
let constas = {
  sucess: "success",
};

let responseObject = response && response.responseCode;
// console.log(responseObject);
console.log(response.le)

